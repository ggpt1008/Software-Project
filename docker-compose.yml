services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: dillibus
      POSTGRES_PASSWORD: dillibus_password
      POSTGRES_DB: dillibus
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
